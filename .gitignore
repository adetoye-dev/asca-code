# ── Operating System & Editor Files ───────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
[Dd]esktop.ini

# Editors & IDEs
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace
*.sublime-project
*.swp
*.swo
*~
*.bak

# ── Node.js & Frontend Build (Vite / React) ──────────────────────────────────
node_modules/
dist/
dist-ssr/
build/
*.local
.npm/
.eslintcache
.stylelintcache
.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Rust & Tauri Desktop ─────────────────────────────────────────────────────
target/
.tauri/target/
.tauri/gen/
gen/
# Cargo.lock is deliberately tracked. This is an application, not a library, and
# without it CI resolves the Rust graph from scratch on every run. That is how
# the desktop bundle broke with nobody touching a dependency: tauri-plugin-updater
# 2.12.0 landed upstream, the unpinned crate requirement picked it up, and Tauri
# then refused to build against the 2.11.0 npm package. The npm side has always
# been pinned by package-lock.json; this is the same guarantee for the crate side.
**/*.rs.bk

# ── Python Runtime & Tooling ─────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual Environments
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# Testing, Type Checking & Linting Caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
htmlcov/
.hypothesis/

# Packaging
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ── Local AI Models & Sidecar Server Caches ─────────────────────────────────
# Ignore huge downloaded weights (.gguf / .bin) & sidecar runtime files,
# but keep directory structures via .gitkeep
core-engine/models/bin/*
!core-engine/models/bin/.gitkeep

core-engine/models/cache/*
!core-engine/models/cache/.gitkeep

# Model files & weights
*.gguf
*.bin
*.pt
*.safetensors
*.onnx

# Sidecar runtime locks and logs
.sidecar.pid
sidecar.log
*.download
*.part

# ── Execution Sandbox & Verification Gauntlet Artifacts ─────────────────────
.acsa/
# Ignore temporary runtime files created during sandbox test runs
*_sandbox_stdout.log
*_sandbox_stderr.log
sandbox_audit_*.jsonl
ide_staging_*
ide_perf_staging_*
aide_sandbox_*
*.tmp
tmp*

# Local environment: credentials and machine-specific overrides.
# `.env.example` is the tracked template.
.env
.env.*
!.env.example

# App database (created under the user data dir by default, but a repo-local
# ACSA_DATA_DIR would put it here).
acsa.db
acsa.db-wal
acsa.db-shm

# Frozen engine sidecar (built by scripts/build_engine_sidecar.sh; 10 MB+)
.tauri/binaries/
.tauri/engine/
.tauri/engine-codex/
